export default async function getWeatherData(city = "New York"){
    // fetch data from API here, 
    // pass the "city" variable into the URLSearchParams() method
    //convert the object retrieved into a usable javascript object
    //THROW errors if needed, but CATCH them elsewhere (ui-controller)
    


    // return { currentTime, cityName, humidity, skyCondition, weatherIcon, temperature }
}